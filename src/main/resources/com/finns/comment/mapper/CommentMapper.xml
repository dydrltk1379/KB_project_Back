<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.finns.comment.mapper.CommentMapper">

    <!-- 소비내역에 있는 댓글 리스트 조회   -->
    <select id="getCommentByUser" resultType="com.finns.comment.dto.CommentDTO">
        SELECT
            c.comment_no,
            c.post_no,
            c.user_no,
            c.content,
            u.username,
            u.img_url
        FROM
            comment c
                JOIN
            user u ON c.user_no = u.user_no
        WHERE
            c.post_no = #{postNo}
    </select>

    <!-- 특정 게시물의 댓글 조회 쿼리 -->
    <insert id="saveComment" parameterType="com.finns.comment.dto.CommentDTO">
        INSERT INTO comment (post_no, user_no, content)
        VALUES (#{postNo}, #{userNo}, #{content});
    </insert>
</mapper>
